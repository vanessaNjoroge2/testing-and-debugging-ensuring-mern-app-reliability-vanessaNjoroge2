{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "description": "Backend for MERN Bug Tracker",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "cross-env NODE_ENV=test jest --detectOpenHandles --forceExit",
    "test:unit": "cross-env NODE_ENV=test jest --detectOpenHandles --forceExit --testPathPatterns=tests/unit",
    "test:integration": "cross-env NODE_ENV=test jest --detectOpenHandles --forceExit --testPathPatterns=tests/integration"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^30.2.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
